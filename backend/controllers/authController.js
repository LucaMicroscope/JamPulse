function register(req, res) {
    res.json({ message: 'Rotta raggiunta con successo' })
}

function login(req, res) {
    res.json({ message: 'Rotta raggiunta con successo' })
}

module.exports = {
    register,
    login
}